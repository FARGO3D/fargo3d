#include "fargo3d.h"

real Resistivity (real y, real z) {
  //The user can freely redefine his own function, in a file named
  //resistivity.c, in his own setup directory, which will be used at
  //built time instead of this file.

  return ETA;
}

